definitions:
  fp.vault.shared_folder_count:
    label: Shared Folder Count
    unit: count
    icon: Lock
    icon_color: "#10b981"
    rollup_strategy: last
    entity_kinds: [project]
    time_kind: realtime
    default_roles_allow: [admin]

